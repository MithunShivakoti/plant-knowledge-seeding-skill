# Secrets — never commit
.env
gcp_credentials.json

# Downloaded permit PDFs — large binaries, regenerated by pipeline
downloads/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Root-level seed package outputs — belong in cache/ not root
*_seed_package.json
!cache/*_seed_package.json
